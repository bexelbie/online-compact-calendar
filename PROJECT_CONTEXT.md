---
kind: code
purpose: online compact-calendar web tool — static frontend + Azure Functions API (api/), deployed via Azure Static Web Apps → cc.bexelbie.com
visibility: public
---

# online-compact-calendar — project context

Public web tool: a static frontend plus an Azure Functions API under `api/`, deployed as an
Azure Static Web App.

## Pointers
- Azure deploy topology — the `compact-calendar` / `compact-calendar-beta` Static Web Apps,
  their hostnames, and resource group `rg-compact-calendar` — is kept **privately**, not in
  this public repo → `~/repos/infra/systems/cloud-azure.md` (private; canonical for what
  exists in Azure).
